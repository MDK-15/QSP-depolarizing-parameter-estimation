# QSP-depolarizing-parameter-estimation - Amplitude Damping
This branch tests the same algorithm on an amplitude damping channel.
We compare the performance of the algorithm against a baseline that starts at the optimal state. This shows the ability of the algorithm to find the optimal state when starting with a non-optimal state.
